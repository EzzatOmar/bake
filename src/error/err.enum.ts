/**
 * Error codes are unique identifiers for the error that occurred.
 * Use dot notation to indicate the module and the function that caused the error. E.g. "CTRL.USER.LIST.FAILED"
 * Alway include module name if possible. E.g. "FN.SYSTEM.ON_START_CHECKS.MISSING_SERVER_FILE"
 * 
 * Check usage via grep
 */

export enum ErrCode {

}