---
description: Analyze and display API usage graph
---

!`bun .opencode/scripts/code-graph.ts`

This shows which API endpoints are referenced in src/index.tsx, which HTTP methods are implemented for each endpoint, and which controllers handle the business logic.

If the user has not given you any addional task end the chat. As this is intendend to preload your
context for a future task.